# Gate Eval For Skills

Place skill-oriented eval and benchmark assets here when they are useful for
measurement but should stay out of the default validation gate.

Shared protocol:

- `gate_eval/skills/protocol.md`

Each skill-owned slice should keep its case truth local even when it reuses the
shared maintainer harness in `dev/eval/`.
