# Design Core Eval

Non-gating eval slice for `bagakit-design-core`.

The initial suite checks deterministic contract coverage: the design-core
contract must expose the tone, rule, packet, and three-checkpoint review
surfaces that downstream skills can consume.
