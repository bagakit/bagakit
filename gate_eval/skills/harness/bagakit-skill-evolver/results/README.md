# Evolver Eval Results

Eval runs write sanitized results here.

Checked-in files should stay limited to:

- `.gitignore`
- `README.md`

Per-run outputs belong under `runs/` and should not be committed.
