version = 2

[[suite]]
id = "bagakit-spark-behavior-starter-eval"
owner = "gate_eval/skills/harness/bagakit-spark"
description = "Run deterministic non-gating eval for Spark closure, option-surface, and evidence-route review surfaces."
default = true
proof_mode = "runtime"
protects = ["spark review behavior for accepted snapshot closure, weak questions, option surfaces, sufficient evidence routing, and next action handoff"]
oracle = ["shared dev/eval runner executes the Spark behavior starter suite against deterministic fixtures"]
exercised_surface = ["gate_eval/skills/harness/bagakit-spark/suite.ts", "skills/harness/bagakit-spark/references/review-packet-template.md"]
proves = ["bagakit-spark-behavior-starter-eval proves deterministic Spark closure, option-surface, and evidence-route rules are represented by public workflow contracts."]
does_not_prove = ["bagakit-spark-behavior-starter-eval does not prove live dialogue quality, route judgment, prototype quality, or release-blocking correctness."]
timeout_seconds = 120
validation_class = "quality"
groups = ["skills", "harness", "spark", "eval", "quality"]

[suite.runner]
kind = "executable"
command = "{node}"
args = [
  "--experimental-strip-types",
  "dev/eval/src/cli.ts",
  "run",
  "--root",
  "{repo_root}",
  "--suite",
  "gate_eval/skills/harness/bagakit-spark/suite.ts",
]
