# Runtime pipeline

The network 链路 records packet timing. The control 闭环 is part of the simulator,
and the matrix operation is documented in the API reference.

The implementation uses robust retry behavior.
