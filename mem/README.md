# Memory

This directory holds durable repository memory that still evolves and should
not yet be promoted into stable specifications or runtime skill payload.

Rules:

- do not use `mem/` as a junk drawer
- keep only records with future reuse value
- promote stabilized rules into `docs/`
- keep executable tooling out of `mem/`; that belongs under `dev/`

Current memory areas:

- `brainstorm-memory-quality/`
- `migration/`
- `decisions/`
- `benchmarks/`
