# MR Title Template: Outcome-First

Use when:

- the change fixes a concrete reviewer-visible or user-visible problem
- the outcome matters more than the implementation detail

Template:

```text
<type>: <outcome> for <scope>
```
