# Feature Tasks: <feature-id>

This markdown file is a human-readable view. JSON SSOT lives in `tasks.json`.

## Task Checklist
- [ ] T-001 Implement first scoped change for this feat

## Notes
- Keep one `in_progress` task at a time.
- Use `feature-tracker.sh run-task-gate` before commit.
