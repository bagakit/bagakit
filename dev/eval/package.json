{
  "name": "bagakit-eval",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "bash ../host_tools/typescript/tsc-local.sh --root ../.. -- -p dev/eval/tsconfig.json --noEmit"
  }
}
