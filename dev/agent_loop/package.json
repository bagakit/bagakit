{
  "name": "@bagakit/agent-loop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Maintainer-only outer driver for Bagakit flow-runner sessions.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "bash ../host_tools/typescript/tsc-local.sh --root ../.. -- -p dev/agent_loop/tsconfig.json --noEmit"
  }
}
