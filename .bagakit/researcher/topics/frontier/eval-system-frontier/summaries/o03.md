# OpenAI optimizer loop constraints

## What This Is

- source id: `O03`

## Why It Matters

it shows what an optimizer consumes when eval becomes an optimization substrate

## Borrow
- reuse the same normalized dataset and grader surfaces instead of inventing a parallel optimizer schema

## Avoid
- hiding optimization behind opaque score-only loops

## Bagakit Implication
- Bagakit can host optimization later without rewriting the eval data model
